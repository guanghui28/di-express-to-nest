export const METADATA_KEY = {
  METHOD: 'method-metadata-key',
  CONTROLLER: 'controller-metadata-key',
  PARAM: 'param-metadata-key',
  INJECTABLE: 'injectable-metadata-key',
} as const;

export const HTTP_METHODS = {
  GET: 'get',
  POST: 'post',
  PATCH: 'patch',
  PUT: 'put',
  DELETE: 'delete',
} as const;
